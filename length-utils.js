
//Returns Array Length, like ColdFusion's arraylen() function:
function arraylen(array) {
  return array.length;
}

//Returns String Length, like ColdFusion's len() function:
function len(string){
  return string.len;
}